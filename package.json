{
  "name": "interview-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend/interview && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "dependencies": {},
  "devDependencies": {}
}