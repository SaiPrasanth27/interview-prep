{
  "buildCommand": "cd frontend/interview && npm install && npm run build",
  "outputDirectory": "frontend/interview/dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}